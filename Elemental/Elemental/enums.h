#pragma once


enum PATHING {
	PATHABLE,
	NOT_PATHABLE,
	DOORWAY
};

enum ELEMENT {
	FIRE,
	WATER,
	EARTH,
	AIR,
	BLOOD,
	LIGHT,
	VOID
};

enum TARGET_TYPE {
	POINT,
	ENEMY
};

enum SPELL {
	FIREBOLT
};
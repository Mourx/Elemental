#pragma once


enum PATHING {
	PATHABLE,
	NOT_PATHABLE,
	DOORWAY
};

enum ELEMENT {
	FIRE,
	WATER,
	EARTH,
	AIR,
	BLOOD,
	LIGHT,
	VOID
};

enum TARGET_TYPE {
	POINT,
	ENEMY,
	SELF
};

enum EFFECT_TYPE {
	EFFECT_SELF,
	EFFECT_STATIC
};

enum SPELL {
	FIREBOLT,
	WATERBALL,
	WATERPULSE,
	LIGHTNINGSTRIKE
};
